import header from "@/components/header/Header";
import carnets from "@/components/carnets/Carnets";

const Components = [
  { name: 'app-header', template: header },
  { name: 'carnets', template: carnets },
]

export default Components
